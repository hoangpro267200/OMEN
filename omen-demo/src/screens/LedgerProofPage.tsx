import { LedgerProofScreen } from './LedgerProofScreen';

/**
 * Ledger Proof page — WAL framing and crash-tail safety demo.
 */
export function LedgerProofPage() {
  return <LedgerProofScreen />;
}
